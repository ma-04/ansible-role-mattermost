[defaults]
inventory = inventory.ini
# The repo root IS the role; its parent dir is the roles container so the
# role resolves by the directory name "ansible-role-mattermost".
roles_path = ../..
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
